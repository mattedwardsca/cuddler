@using global::Cuddler.Pages.Shared.Cuddler.FormTooltip
@using global::Cuddler.Utils
@model FormLabelTagHelper

@if (!string.IsNullOrEmpty(Model.Text))
{
    <label for="@Model.ForProperty" @Model.Block.IsTrueHtml("class='d-block'")>
        @Html.Raw(Model.Text)
        @if (Model.Required)
        {
            <span class="eux-required">*</span>
        }
    </label>
    <form-tooltip title="@Model.Description" />
}
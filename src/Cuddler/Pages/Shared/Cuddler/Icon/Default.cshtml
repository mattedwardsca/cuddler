@using global::Cuddler.Core.Utils
@using global::Cuddler.Web.Helpers
@model IconTagHelper
@{
    var cssClass = EFontAwesomeIconHelper.ToString(Model.Type);
    var cssSize = FontAwesomeUtil.ToString(Model.Size);
}
<span class="eux-Icon">
    <i class="@cssClass @cssSize"></i>
</span>
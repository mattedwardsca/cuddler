@using global::Cuddler.Core.Utils
@using Kendo.Mvc.UI
@model int
@{
    var webId = WebIdUtil.GetWebId();
    var workItems = TimeTypeDropdownUtil.List();
}
@*@(Html.Kendo()
      .IntegerTextBoxFor(m => m))*@
<script>
    function @(webId)_assignedToFocus(e) { e.sender.open(); }
</script>
@(Html.Kendo()
      .DropDownListFor(m => m)
      .BindTo(workItems)
      .Events(e => e.DataBound($"{webId}_assignedToFocus")))